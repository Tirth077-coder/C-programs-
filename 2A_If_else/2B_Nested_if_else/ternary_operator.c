#include <stdio.h>
int main()
{
    // NO TO USE JUST FOR KNOWLEDGE
    // WE TAKE EXAMPLE BY EVEN ODD CODE
    int x;
    printf("ENTER THE NUMBER :");
    scanf("%d",&x);


    // TERNARY OPERATOR
    // EXP1 ? EXP2 : EXP 3
    //if-condition ? printf : else-printf
      x%2==0 ? printf("evven") : printf("odd");

    return 0;
}